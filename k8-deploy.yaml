# Creates a deployment set 
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shipping
  labels:
    app: shipping
spec:
  replicas: 2
  selector:
    matchLabels:
      app: shipping 
  template:
    metadata:
      labels:
        app: shipping
    spec:
      containers:
      - name: shipping
        image: 355449129696.dkr.ecr.us-east-1.amazonaws.com/shipping:4.0.1
        ports:
        - containerPort: 80 
--- 

apiVersion: v1
kind: Service
metadata:
  name: shipping-svc
spec:
  # type: LoadBalancer
  selector:
    app: shipping
  ports:
    - protocol: TCP
      port: 80              # Port number of the service that it has to listen on 
      targetPort: 80        # Port number of the container that it has to forward to

---
# Ingress Resource

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-myservicea
spec:
  rules:
  - host: shipping.robos-shop.online
    http:
      paths:
      - path: "/"
        pathType: Prefix
        backend:
          service:
            name: shipping-svc
            port:
              number: 80
  ingressClassName: nginx